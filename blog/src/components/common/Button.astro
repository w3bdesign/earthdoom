---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'button'> {
  variant?: "primary" | "secondary" | "ghost";
  size?: "sm" | "md" | "lg" | "xl";
}

const {
  variant = "primary",
  size = "md",
  class: className = "",
  ...props
} = Astro.props;

const sizeClasses = {
  sm: "px-4 py-2 text-sm min-w-[100px]",
  md: "px-6 py-2.5 text-base min-w-[140px]",
  lg: "px-8 py-3 text-base min-w-[180px]",
  xl: "px-10 py-4 text-lg min-w-[240px]",
};

const variantClasses = {
  primary: `
    bg-gradient-to-r from-green-500 via-green-600 to-emerald-600
    hover:from-green-400 hover:via-green-500 hover:to-emerald-500
    text-white font-bold
    shadow-xl shadow-green-900/30
    hover:shadow-2xl hover:shadow-green-500/40
    focus:ring-4 focus:ring-green-400/50
    hover:scale-105
  `,
  secondary: `
    bg-transparent border-2 border-white/60
    hover:bg-white/10 hover:border-white
    text-white font-medium
    shadow-lg hover:shadow-xl hover:shadow-white/20
    focus:ring-4 focus:ring-white/30
    backdrop-blur-sm
  `,
  ghost: `
    bg-transparent
    hover:bg-white/5
    text-white/80 hover:text-white
    font-medium
    focus:ring-2 focus:ring-white/20
  `,
};
---

<button
  class={`
    relative overflow-hidden
    rounded-lg
    transition-all duration-300 ease-out
    focus:outline-none
    active:scale-95
    ${sizeClasses[size]}
    ${variantClasses[variant]}
    ${className}
  `}
  {...props}
>
  <span class="relative z-10">
    <slot />
  </span>
</button>
